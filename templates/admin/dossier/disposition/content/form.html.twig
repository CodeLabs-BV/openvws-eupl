{{ form_start(form) }}
    {{ form_row(form.summary) }}
    <button type="button" class="bhr-btn-bordered-primary mb-4" data-ai-write
            data-ai-token="{{ csrf_token('ai_generate') }}"
            data-ai-context="samenvatting van een Woo-dossier">
        {{ 'admin.ai.write_summary' | trans }}
    </button>

    <twig:Admin:Publication:ManageNoticeNotPublic
        canDelete="{{ hasNoticeNotPublic }}"
        endpoint="{{ '/balie/api/dossiers/' ~ dossier.id ~ '/notice-not-public' }}"
        formField="{{ form.noticeNotPublic }}"
        grounds="{{ grounds }}"
        hasMainDocument="{{ dossier.mainDocument is not null }}"
    />

    <twig:Admin:Publication:ManageDocument
        canDelete="{{ canDeleteDocument }}"
        documentType="{{ 'admin.dossiers.disposition.document' | trans }}"
        documentTypes="{{ documentTypes }}"
        endpoint="{{ '/balie/api/dossiers/' ~ dossier.id ~ '/disposition-document' }}"
        fileLimits="{{ documentFileLimits | default([])}}"
        formField="{{ form.document }}"
        grounds="{{ grounds }}"
        hasNoticeNotPublic="{{ hasNoticeNotPublic }}"
        helpText="{{ 'admin.dossiers.disposition.form.content.document_help' | trans }}"
        languages="{{ attachmentLanguages }}"
        title="{{ 'admin.dossiers.disposition.form.content.document_label' | trans }}"
        dossierId="{{ dossier.id }}"
    />

    <twig:Admin:Publication:ManageAttachments
        canDelete="{{ canDeleteAttachments }}"
        documentTypes="{{ attachmentTypes }}"
        endpoint="{{ '/balie/api/dossiers/' ~ dossier.id ~ '/disposition-attachments' }}"
        fileLimits="{{ attachmentFileLimits | default([])}}"
        grounds="{{ grounds }}"
        helpText="{{ 'admin.dossiers.disposition.form.content.attachments_help' | trans }}"
        languages="{{ attachmentLanguages }}"
        dossierId="{{ dossier.id }}"
    />

{{ form_end(form) }}
